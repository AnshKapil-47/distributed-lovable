spring:
  application:
    name: config-service

  cloud:
    config:
      server:
        git:
          uri: https://github.com/AnshKapil-47/lovable-config-server
          username: AnshKapil47
          password: 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
          default-label: main

server.port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
    config:
      enabled: false

---

spring:
  config:
    activate:
      on-profile: k8s

  cloud:
    discovery:
      enabled: false
    config:
      server:
        git:
          uri: https://github.com/AnshKapil-47/lovable-config-server
          username: {GIT_USERNAME}
          password: {GIT_PASSWORD}

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
